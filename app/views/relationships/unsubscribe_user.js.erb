$('#unfollow-button').text('Subscribe')
                     .attr('class', 'btn follow-button')
                     .attr('href', "/<%= @category.id %>/subscribe_user")
                     .attr('id', 'follow-button');